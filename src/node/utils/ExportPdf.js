// import jsPDF from 'jsPDF'
// //  const pdf = await exporttxt.getPadHTMLDocument(padId, req.params.rev);

// exports.getPadPdfDocument = async (padId, revNum, readOnlyId) => {
//   const pad = await padManager.getPad(padId);

//   var doc = new jsPDF('l', 'mm', [1200, 1210]);
//   return doc.html(pdfjs, {
// 		callback: function(doc) {
// 			doc.save("output.pdf");
// 		},
// 		x: 10,
// 		y: 10
// 	});
// };
